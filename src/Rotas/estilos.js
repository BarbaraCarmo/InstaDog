import { StyleSheet } from "react-native";
import { cores } from '../estilosGlobal'

export default StyleSheet.create({
    icones: {
        marginTop: 5,
        color: cores.branco,
        fontSize: 30,
        padding: 0,
        alignSelf: "center",
        alignItems: "baseline"
    }
})